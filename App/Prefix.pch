/**
 Prefix header
 
 包含在此文件的头文件会在所有文件中包含，不用另引入
 
 但注意不要包含过多的文件，这会减慢编译的速度，因为一但修改这些文件需要重新编译整个项目
 不建议包含除类库意外的文件，项目大了 clean 再重新编译的时间太长，而且编辑这些文件时代吗高亮容易失效，相对于顺手加头文件不值当
 */

#import <Availability.h>

#ifndef __IPHONE_7_0
#warning "This project uses features only available in iOS SDK 7.0 and later."
#warning "项目编译需要 iOS 7 SDK"
#endif

#ifdef __OBJC__
    #import "RFUI.h"
    #import "UIKit+App.h"
#endif
